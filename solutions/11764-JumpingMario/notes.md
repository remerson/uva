Category: Ad Hoc
Difficulty: Trivial

For each of the input rows, count the high/low difference between each consecutive number.

Note that the size of N can be 0, so skip calculating in this case.

To improve efficiency, the items can be read into a buffer and then iterated through, rather than calling scanf each time.
